function setup() {
    let canvas = createCanvas(100, 100);
    // Asociar el canvas al contenedor específico en el HTML
    canvas.parent('contenedor-canvas-p5');
}

function draw(){
    background(0);
}